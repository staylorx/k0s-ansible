[project]
name = "ansible-control"
version = "0.1.0"
description = ""
authors = [
    {name = "Steve Taylor",email = "staylorx@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
package-mode = false

dependencies = [
    "ansible (>=11.6.0,<12.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
ansible-lint = "^25.5.0"

